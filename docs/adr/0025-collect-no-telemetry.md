# Collect no telemetry

Version 1 will collect no usage telemetry and perform no automatic crash-report uploads. Logs, diagnostics, model responses, and document-derived artifacts remain inside the Conversion Bundle unless the user deliberately shares them; at conversion runtime, the only document-bearing network traffic is the explicitly consented model traffic to the configured provider.
